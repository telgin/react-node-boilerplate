import {connect} from 'react-redux';
import {AppRoot} from './app-root';

const mapStateToProps = (state) => {
    return {

    };
};

const dispatchProps = {

};

export const AppRootConnector = connect(mapStateToProps, dispatchProps)(AppRoot);